/** @type {import('tailwindcss').Config} */
export const content = [
  "./src/**/*.{html,js,ts}", // Incluye todos los archivos .html, .js, y .ts en el directorio src
  "./src/app/**/*.{html,js,ts}" // Asegúrate de incluir también el directorio app si es necesario
];
export const theme = {
  extend: {},
};
export const plugins = [];
